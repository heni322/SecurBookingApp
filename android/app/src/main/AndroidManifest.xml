<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Android 13+ (API 33) — required for FCM to prompt and display notifications -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Camera — check-in / check-out photos and dispute attachments -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!--
      Photo selection. On Android 13+ the scoped media permissions are used;
      on older versions READ_EXTERNAL_STORAGE (capped at API 32) is the fallback.
    -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!--
      CAMERA is requested at runtime, not required to install — declaring the
      hardware feature as optional keeps the app installable on camera-less devices.
    -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:networkSecurityConfig="@xml/network_security_config"
      android:usesCleartextTraffic="false"
      android:supportsRtl="true">

      <!--
        react-native-maps: The Google Maps SDK (play-services-maps) is always compiled
        as a native dependency and initializes at app startup on Android.
        It requires this meta-data tag to be present or it throws IllegalStateException,
        even when using OSM tiles only (UrlTile) with no PROVIDER_GOOGLE.
        Empty value = SDK initializes without hitting Google Maps Platform.
        Actual map tiles come from OSM via UrlTile + mapType="none" in MapView.
      -->
      <meta-data
        android:name="com.google.android.geo.API_KEY"
        android:value="" />

      <!--
        FCM default notification channel for Android 8+ (O). The channel id must
        match the one created natively / by the notifications service so that
        background notifications are routed to a visible, user-configurable channel.
      -->
      <meta-data
        android:name="com.google.firebase.messaging.default_notification_channel_id"
        android:value="provalk_default"
        tools:replace="android:value" />

      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>

        <!-- Deep-link scheme: securbook://… (notification tap-through, email links) -->
        <intent-filter android:autoVerify="false">
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.DEFAULT" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="securbook" />
        </intent-filter>
      </activity>
    </application>
</manifest>
